<?php

/**
 * Return layout key 'grid-layout'.
 *
 * Used as shortcut second parameter for `add_filter()`.
 *
 * @since 1.7.0
 *
 * @return string `'grid-layout'`.
 */
function __genesis_return_grid_layout() {

	return 'grid-layout';

}

/**
 * Return layout key 'timeline-layout'.
 *
 * Used as shortcut second parameter for `add_filter()`.
 *
 * @since 1.7.0
 *
 * @return string `'timeline-layout'`.
 */
function __genesis_return_timeline_layout() {

	return 'timeline-layout';

}

/**
 * Return layout key 'content-layout'.
 *
 * Used as shortcut second parameter for `add_filter()`.
 *
 * @since 1.7.0
 *
 * @return string `'content-layout'`.
 */
function __genesis_return_content_layout() {

   return 'content-layout';

}